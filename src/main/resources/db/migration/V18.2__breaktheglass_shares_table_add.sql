CREATE TABLE IF NOT EXISTS break_the_glass_shares (
    id VARCHAR(55),
    description VARCHAR(255),
    who_create VARCHAR(255),
    credential_id VARCHAR(255),
    created_at TIMESTAMP,
    expired_at TIMESTAMP
);